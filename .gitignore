# Build artifacts (cache & output) — regenerated by Gradle, not project files
.gradle
build

# Runtime working directory
/run

# IDE
.idea/

# Upstream Paper source trees (DMCA-sensitive; working dirs regenerated by
# Paperweight during build — must NOT be committed)
/paper-api
/paper-server
